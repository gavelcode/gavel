name: example-go-repo
projects:
    - name: api-gateway
      pattern: //...
      tooling:
        go: [golangci-lint, archtest]
      quality_gate:
        findings:
            max_error: 0
        coverage:
            min: 90
        architecture_violations:
            max: 0
